/**
 * @cornerman/ui · 组件库入口（占位）
 *
 * 后续将承接 Coach Lab 设计的响应式组件：
 * Button / Card / StatPill / RevisionBadge / SegmentTimeline / ProblemThreadItem 等。
 */
export { tokens } from "./tokens";
export type { Tokens } from "./tokens";
